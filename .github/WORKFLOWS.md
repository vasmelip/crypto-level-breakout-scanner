# 🔄 GitHub Actions Workflows

Автоматические работы для КО и развёртывания.

## 📋 Документация

### 1. **Deploy to GitHub Pages** (`.github/workflows/deploy.yml`)

Основной workflow для автоматического развёртывания.

#### Описание:
- Рунное время: Около 1-2 минут
- Триггеры:
  - Push в `main` branch
  - Pull requests в `main` branch

#### Процесс:
1. Клонирует репозиторий
2. Проверяет наличие `index.html`
3. Проверяет `.nojekyll`
4. Настраивает GitHub Pages
5. Загружает артифакты
6. Востанавливает приложение

#### Результат:
```
https://vasmelip.github.io/crypto-level-breakout-scanner/
```

---

### 2. **Code Quality Checks** (`.github/workflows/lint.yml`)

Контроль качества кода на каждый push и pull request.

#### Описание:
- Рунное время: Около 30 сек
- Триггеры: На каждый push/PR

#### Проверки:
- ✅ Наличие `index.html`
- ✅ DOCTYPE декларация
- ✅ Meta charset (UTF-8)
- ✅ Viewport тег
- ✅ `<style>` тег
- ✅ `<script>` тег
- ✅ CSS переменные
- ✅ JavaScript функции:
  - `analyzePattern()`
  - `drawChart()`
  - `scanPair()`
  - `displayResults()`
  - `clearResults()`
- ✅ DEMO_DATA данные
- ✅ Test data (BTC/USDT, ETH/USDT)

#### Результат:
В Actions повышаются зеленые тычки (✅).

---

### 3. **Create Release** (`.github/workflows/release.yml`)

Автоматическое создание releases при тэгах.

#### Описание:
- Триггер: Push тэга `v*` (v1.0.0, v1.0.1, и т.д.)
- Автоматическая генерация Release Notes
- Загружает assets

#### Использование:
```bash
# 1. Наомияем тэг
sgit tag v1.0.0

# 2. Пушим тэг
git push origin v1.0.0

# 3. GitHub Actions автоматически создаст Release
```

#### Результат:
Новый Release в [Releases](https://github.com/vasmelip/crypto-level-breakout-scanner/releases)

---

## 📑 Этапы CI/CD

```
┌─────────────────────────────────────────────────────┐
│  Push/PR to main or Tag creation                    │
└──────────────────┬──────────────────────────────────┘
                   │
         ┌─────────┴──────────┐
         │                    │
         ▼                    ▼
  ┌──────────────┐    ┌──────────────┐
  │ Code Quality │    │   Deploy to  │
  │   Checks     │    │GitHub Pages  │
  │ (lint.yml)   │    │(deploy.yml)  │
  └──────┬───────┘    └──────┬───────┘
         │                   │
         └─────────┬─────────┘
                   │
                   ▼
          ✅ Status updated
         (Green/Red badge)
                   │
                   ▼ (on tag push)
          ┌────────────────────┐
          │  Create Release    │
          │  (release.yml)     │
          └────────────────────┘
```

---

## 🛠 Управление Workflows

### Просмотр статуса:
```
гитоб → Actions tab → увидите все руны
```

### Остановка workflow:
1. Settings → Actions → General
2. "Disable all workflows"

### Пояснение выполнения:
1. Клик на workflow run
2. Клик на job
3. Открытся полные логи

---

## 🛰 Траблшутинг

### Deploy workflow не сохраняется:
```bash
# 1. Проверьте Settings → Pages
# 2. Build source = Deploy from branch
# 3. Branch = main
# 4. Folder = / (корень)
```

### Lint checks не проходят:
```bash
# 1. Проверьте index.html существует
# 2. Проверьте все функции
# 3. Проверьте demo data
```

---

## 🚀 Отличные практики

1. Оснащаете коммиты когда меняете index.html
2. Тегуйте с семверонингом (v1.0.0, v1.1.0, v2.0.0)
3. Проверьте Actions перед выпуском
4. Написывайте описание изменений

---

## 💫 Проблемы?

Откройте [Issues](https://github.com/vasmelip/crypto-level-breakout-scanner/issues)
